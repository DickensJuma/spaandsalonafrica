services:
  app:
    image: ghcr.io/dickensjuma/spaandsalonafrica:latest
    env_file:
      - .env
    environment:
      PORT: 8080
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
